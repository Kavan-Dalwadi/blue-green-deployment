apiVersion: apps/v1
kind: Deployment
metadata:
    name: prod-blue
spec:
    selector:
        matchLabels:
            app: example
            env: blue
    replicas: 1
    template:
        metadata:
            labels:
                app: example
                env: blue
        spec:
            containers:
                - name: example-api
                  imagePullPolicy: Always
                  image: kdalwadi28/nginx-prod
                  ports:
                      - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
    name: prod-blue
spec:
    type: NodePort
    selector:
        app: example
        env: blue
    ports:
        - port: 5000
          targetPort: 5000
          nodePort: 30007
