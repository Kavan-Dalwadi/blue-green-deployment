apiVersion: apps/v1
kind: Deployment
metadata:
    name: stage
spec:
    selector:
        matchLabels:
            app: example
            env: blue
    replicas: 1
    template:
        metadata:
            labels:
                app: example
                env: blue
        spec:
            containers:
                - name: example-api
                  imagePullPolicy: Never
                  image: dipak:latest
                  ports:
                      - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
    name: stage
spec:
    selector:
        app: example
        env: blue
    ports:
        - port: 5000
          targetPort: 5000
